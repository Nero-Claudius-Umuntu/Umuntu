---
# Umuntu Final Configuration Module
# "Umu! ¡Qué hermoso es este sistema final!" - Nero Claudius

job:
  - umuntu-final

script:
  - "#!/bin/bash"
  - "set -e"
  - "echo '🌹 Finalizing Umuntu - Nero Claudius Edition 🌹'"
  - ""
  - "# Set Umuntu as default distribution"
  - "echo 'Umuntu 1.0.0 (Nero Claudius Edition)' > /etc/os-release"
  - "echo 'Umuntu' > /etc/hostname"
  - ""
  - "# Configure system information"
  - "echo 'Umuntu - Nero Claudius Edition' > /etc/issue"
  - "echo 'Umu! ¡Qué hermoso es este sistema!' >> /etc/issue"
  - ""
  - "# Clean up"
  - "rm -f /etc/machine-id"
  - "rm -f /var/lib/dbus/machine-id"
  - "rm -f /etc/ssh/ssh_host_*"
  - "rm -rf /tmp/*"
  - "rm -rf /var/tmp/*"
  - "rm -rf /var/log/*"
  - "rm -rf /var/cache/apt/archives/*"
  - "rm -rf /var/lib/apt/lists/*"
  - ""
  - "echo 'Umuntu finalization completed successfully!'"
  - "echo 'Umu! ¡Qué hermoso es este sistema final!'"
  - "echo '🌹 Umuntu - Nero Claudius Edition está listo! 🌹'"
