---
# Mount module configuration
# This module mounts the target filesystem

# Mount options
mountOptions:
    - "defaults"
    - "noatime"

# Filesystem types to mount
filesystemTypes:
    - "ext4"
    - "xfs"
    - "btrfs"
    - "f2fs"

# Mount point for the target
targetMountPoint: "/target"
